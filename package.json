{
  "name": "talking-head",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter @talking-head/demo dev",
    "lint": "pnpm -r run lint",
    "generate:demo-audio": "node scripts/generate-demo-audio-fixture.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
